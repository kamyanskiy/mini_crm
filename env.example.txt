# Application
PROJECT_NAME=mini_crm

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=crm_database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Cache
UNIT_CACHE_EXPIRE_IN_SECONDS=300
VIEW_CACHE_EXPIRE_IN_SECONDS=3600

# JWT
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Initial Admin Setup (used by 'crm-admin init' command in entrypoint.sh)
# Set to true to automatically create admin user on first startup
CREATE_ADMIN_ON_STARTUP=false
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123
ADMIN_NAME=System Administrator
ADMIN_ORGANIZATION=Default Organization
